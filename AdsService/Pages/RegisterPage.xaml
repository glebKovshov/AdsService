<Page x:Class="AdsService.Pages.RegisterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Регистрация"
      FontFamily="Tw Cen MT"
      FontSize="12"
      Background="White">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#37474F" />
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#78909C" />
        <SolidColorBrush x:Key="AccentBrush" Color="#FF9C1A" />
    </Page.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="10" Margin="0,0,0,16" Background="White"> 
            <DockPanel>
                <Button Content="Назад"
                        Width="90"
                        Height="30"
                        Margin="0,0,16,0"
                        Click="BtnBack_Click"/>

                <Image Source="/Resources/Лого.png"
                       Width="40"
                       Height="40"
                       Margin="0,0,16,0"
                       Stretch="Uniform" />

                <TextBlock Text="Регистрация"
                           VerticalAlignment="Center"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="{StaticResource PrimaryTextBrush}" />
            </DockPanel>
        </Border>

        <!-- Content -->
        <Border Grid.Row="1" Padding="16" Background="White" BorderThickness="1" BorderBrush="#FFE0E0E0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Width="380" HorizontalAlignment="Left">

                <TextBlock Text="Логин"
                           Foreground="{StaticResource SecondaryTextBrush}"
                           Margin="0,0,0,6"/>

                <TextBox x:Name="TxtLogin"
                         Height="38"
                         Padding="10"
                         VerticalContentAlignment="Center"
                         Margin="0,0,0,16"/>

                <TextBlock Text="Пароль"
                           Foreground="{StaticResource SecondaryTextBrush}"
                           Margin="0,0,0,6"/>

                <PasswordBox x:Name="PwdPassword"
                             Height="38"
                             Padding="10"
                             Margin="0,0,0,16"/>

                <TextBlock Text="Повтор пароля"
                           Foreground="{StaticResource SecondaryTextBrush}"
                           Margin="0,0,0,6"/>

                <PasswordBox x:Name="PwdRepeat"
                             Height="38"
                             Padding="10"
                             Margin="0,0,0,16"/>

                <Button Content="Зарегистрироваться"
                        Width="180"
                        Height="38"
                        Background="{StaticResource AccentBrush}"
                        BorderBrush="{StaticResource AccentBrush}"
                        Foreground="White"
                        Click="BtnRegister_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
